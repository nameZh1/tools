# Debug Mode Rules

## 非显而易见的调试规则

- **端口冲突**: 如果服务器启动失败，检查端口 3000+ 是否被占用，服务器会自动递增端口
- **文件上传失败**: 确认 `shared_files/` 目录存在且有写入权限
- **中文文件名乱码**: 问题在 multer 处理层，检查 [`server.js:24`](server.js:24) 的 Buffer 转换
- **文本保存失败**: 检查 `shared_text.txt` 文件权限和磁盘空间
- **前端不刷新**: 文本框有 1 秒防抖，等待或手动点击刷新按钮
